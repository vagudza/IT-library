TESTING
1 файл с тестами должен иметь префикс _test.go
2 функция в файле с тестами должна начинаться с Test_


func Test_Calc(t *testing.T) {
    t.Parallel()                                    // запуск тестов параллельно с остальными тестами

    t.Run("test name", func(t *testing.T){          
        t.Parallel()
        result := 3

        res := CalcSum(1, 2)

        assert.Equal(t, result, res)
    })
}


// Табличный тест. Для генерации необходимо ПКМ->Go: generate unit test for the function


// Mock в go - генерируемый с помощью GoMock
    - используется не часто, т.к. зачем тестировать с заглушкой (например, БД), если можно протестировать напрямую код работы с базой?
    - заглушка всегда даст "ожидаемый" результат. 
    - для теста с базой лучше использовать интеграционные тесты

// Пример



